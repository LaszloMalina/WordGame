<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lacee.wordgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Welcome"
            android:label="@string/welcome_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".StartScreen"
            android:label="@string/start_sceen">

        </activity>
        <activity
            android:name=".Process"
            android:label="@string/activity_process">
        </activity>
        <activity
            android:name=".Correct"
            android:label="@string/label_correct">
        </activity>
        <activity
            android:name=".Incorrect"
            android:label="@string/label_incorrect">
        </activity>
        <activity
            android:name=".Duplicate"
            android:label="@string/label_duplicate">
        </activity>
    </application>

</manifest>